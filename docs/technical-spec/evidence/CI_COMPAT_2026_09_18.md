# CI-COMPAT-05 — Remote Protobuf compatibility evidence — 2026-09-18

## Result

`Protobuf Contract` passed on the GitHub-hosted Ubuntu runner for commit
`f53894f189ea69a131cd40fe2cb25dbc1b3f1010`.

- Workflow run: [CI #49](https://github.com/Rochthii/standalone-policy-engine/actions/runs/35330805718)
- Job: `Protobuf Contract`
- Started: 2026-09-18T09:41:04Z
- Completed: 2026-09-18T09:42:21Z
- `Check protobuf backward compatibility`: `success`

The workflow fetches repository history for this job. On a pull request it
compares the current contract with `.git#branch=${{ github.base_ref }}`; on a
push it compares with `.git#ref=HEAD~1`. Both paths run Buf's configured
`FILE` breaking-change rules from `buf.yaml`.

## Boundary

The recorded remote run exercised the push baseline. The pull-request branch
selection is configured and was locally validated against `main`; it has not
yet been exercised by a separately created pull request.
