# REL-GATE-04 — Production readiness decision (2026-09-17)

## Decision

**NO-GO.** The repository is not approved for a production release.

This assessment applies after commit `a454aec` (`feat: restore edge policy
snapshots`). That commit has not satisfied the required release gates and has
not been accepted as a release candidate.

## Local evidence observed

| Gate | Result | Evidence |
|---|---|---|
| Unit/integration suite | PASS | `go test -count=1 ./...` passed on the current implementation before this documentation-only decision record. |
| Static analysis | PASS | `go vet ./...` passed with that suite. |
| Formatting/diff integrity | PASS | `gofmt -l cmd internal pkg tests` produced no files; `git diff --check` produced warnings only and no errors. |
| Compose manifest validity | PASS | `docker compose -f docker-compose.testbed.yml config --quiet` and `docker compose -f tests/docker-compose.yml config --quiet` both exited 0. |
| Race detector | BLOCKED | `go test -count=1 -race ./internal/... ./pkg/...` cannot run on this Windows host because CGO is unavailable. |

## Release blockers

The following required gates in `PRODUCTION_READINESS_CHECKLIST.md` remain
unchecked and are release-blocking:

- G0: README/provenance/claim-integrity and benchmark-boundary evidence.
- G6: retention, external append-only storage and deletion policy evidence.
- G7: compatibility checks executed in CI.
- G8: container runtime hardening and backup/restore/rollback/key-incident rehearsal.
- G9: race result on the release commit, agreed critical-path coverage gate,
  combined local/remote Odoo E2E evidence, and inspected successful remote CI.

`CURRENT_STATE_AUDIT.md` also retains unresolved P1 findings for linear policy
scans, incomplete production-boundary performance measurement, uninspected
remote CI, and contradictory documents. No result in this record changes those
findings to passed.

## Conditions to reconsider

Re-evaluate only after a single committed release candidate has the missing
checklist evidence, including a C-capable CI `-race` run and an inspected
successful remote CI run. A passing local suite alone is insufficient.
